#!/bin/bash

# Запрос массы
read -p "Введите массу (кг): " weight

# Запрос роста
read -p "Введите рост (м): " height

# Вычисление ИМТ с помощью bc (поддержка плавающей точки)
bmi=$(echo "scale=0; $weight / ($height * $height)" | bc)

# Вывод результата
echo "Ваш индекс массы тела (ИМТ): $bmi"
